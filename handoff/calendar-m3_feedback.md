# Review: Calendar M3 Restyle (Stage 3)
**Date:** 2026-07-05
**Verdict:** NEEDS CHANGES

## Critical/Architectural Issues
- [ ] Rule 1 color-default update is silently negated at call sites: `src/app/admin/calendar/page.tsx:44-45` — `availableSlotColor="#22c55e"` and `dayOffColor="#ef4444"` are still hardcoded as explicit prop values passed to `ModernCalendar`, unconditionally overriding the new `#21A67A`/`#BA1A1A` defaults just changed inside `ModernCalendar.tsx:35-36`. The `/admin/calendar` page — one of the two primary consumers this whole plan is styling — will keep rendering the old non-M3 green/red regardless of the default change. Similarly `src/app/admin/master/schedule/page.tsx:24-25` falls back to the same old hex (`config.availableSlotColor || "#22c55e"`) when the DB field is unset, so any tenant without an explicit override also still gets old colors. The plan's Section 4 only scoped wrapper/rounded-corner edits for these two files and never accounted for these literal-hex call sites, so this isn't a coder deviation — it's a gap in the plan itself that needs the planner to decide whether to update these two literal fallbacks (and possibly the analogous one in `src/app/admin/settings/page.tsx:23-24`, which is outside the 7 reviewed files but shares the same stale hex) to keep the stated M3-color goal intact.

## Minor/Syntax Issues
(none)

## Passed Checks
- [x] `ModernCalendar.tsx` prop defaults changed to literal hex `#21A67A`/`#BA1A1A` (not `var(...)`), correctly avoiding the `color + '40'`-string-concat breakage the plan warned about.
- [x] Emoji → Lucide icon swap (`Globe`/`User`) applied at all three call sites (dropdown trigger, "All Masters (Combined)" row, per-master row); `grep` for 🌐/👤 in the 4 in-scope files returns nothing.
- [x] Segmented Month/Week/Day toggle converted to pill form (`rounded-full`, `p-0.5 gap-0.5`, static `rounded-full` per button, `shadow-sm` on active); the previously per-index corner-logic and its `idx` param are gone — toggle now maps only over the view-type value, no unused-var lint risk.
- [x] Rule 1 (red/green pills → M3 container tokens) applied at all listed sites: WeekView:186/188/241, DayView:182, MonthView:233/250/274; plain `text-red-400/500` → `text-destructive` at MonthView:155 and DayView:145; no leftover `bg-red-*`/`bg-green-*`/`dark:bg-red-900`/`dark:text-red-400` patterns found in the 4 in-scope files.
- [x] Rule 2 (appointment blocks: tonal tint + dark text + left accent border) applied consistently across WeekView:376-377/416-417, DayView:306-307/383-384, MonthView:167-168/183-188 — `text-foreground`, `color+"26"`, `borderLeft:"3px solid "+color` all present as specified; the two "already-correct, untouched" spots (WeekView:403, DayView:352) correctly left alone except for the "#166534"→"#8B4A58" bonus substitution.
- [x] Rule 3 (WeekView available-interval ignoring `availableSlotColor`) fixed — WeekView:324-329 now uses `style={{backgroundColor: availableSlotColor+'1A', borderLeftColor: availableSlotColor}}` with `border-l-[3px]`; DayView's already-correct equivalent (lines ~260-266) correctly left untouched.
- [x] Rule 4 (DayView "+ New Booking" CTA) — `rounded-full` confirmed at DayView:202.
- [x] Coder's self-reported deviation (DayView nested `text-white/80` status-label → `text-muted-foreground`, line 312) verified correct: parent block is now `text-foreground` (dark-on-light), and the nested label correctly uses a muted-but-visible tone, mirroring the same pattern used for MonthView's chevron icon. No other nested `text-white` overrides found nearby that were missed.
- [x] `.custom-scrollbar` correctly defined in `src/styles/globals.css:162-165`, using `var(--border)`/`var(--muted-foreground)` as specified — purely additive, no regression risk.
- [x] All referenced CSS vars exist and are wired into the cascade: `--md-error-container`/`--md-on-error-container`/`--md-success-container`/`--md-on-success-container` defined in `src/styles/m3-tokens.css:17-21` (imported globally via `src/app/layout.tsx:1`); `--border`/`--muted-foreground` defined in `globals.css:382-444` (light/dark); `bg-card`/`--card` confirmed wired through `tailwind.config.ts:48` and `globals.css:382/429`. No dangling/undefined-var risk.
- [x] Confirmed no `.dark` override exists for `--md-success-container` (matches plan's QA note as pre-existing, non-regressive, same as `badge.tsx`'s `success` variant).
- [x] `admin/master/schedule/page.tsx:21` — `rounded-xl`→`rounded-[20px]` applied, `border`/`shadow-sm` kept as specified.
- [x] `admin/calendar/page.tsx:35` — wrapper updated to `bg-card rounded-[20px]`, `-mx-4 -mt-4`/`bg-background` dropped as specified; inner div's redundant `border-t border-border` also removed.
- [x] No JSX restructuring, no new elements, no handler/state/logic changes detected in any of the 3 view files or `ModernCalendar.tsx` — diff is purely className/style-value substitutions (fetch logic, `updateServer`/`toggleOff`/`addShift`/`removeShift`/`groupOverlappingAppointments`, effect hooks, all unchanged).
- [x] `AppointmentModal.tsx`, `ViewAppointmentModal.tsx`, `BulkSettingsModal.tsx` confirmed untouched — still contain the old hardcoded red/green classes and the stray `👤 {m.name}` emoji in `BulkSettingsModal.tsx:312`, exactly as the plan flagged as deferred/out-of-scope.
- [x] `"#166534"` fallback fully replaced with `"#8B4A58"` — no remaining occurrences found in any of the 4 in-scope calendar files.

## Summary
The Stage 3 implementation is a clean, disciplined styling-only diff that faithfully executes nearly every rule in the plan — icon swaps, pill toggle, M3 container-token substitutions, tonal appointment-block tinting, the Rule 3 WeekView fix, the CTA pill radius, `.custom-scrollbar`, and both page wrappers all match the plan's intent, and all referenced CSS custom properties genuinely exist and are wired into the app (no invisible-var runtime trap like the reviewer was asked to specifically hunt for). The coder's one self-reported deviation (nested status-label color in DayView) is correct and well-justified. The single real problem is that `admin/calendar/page.tsx` still passes the old literal `#22c55e`/`#ef4444` hex values directly as props, and `admin/master/schedule/page.tsx` still falls back to the same old hex when the DB config field is empty — both bypass the very default-value fix this plan just made in `ModernCalendar.tsx`, so the M3 tonal green/red goal silently fails to materialize on the pages that actually render this component. This is a plan-scoping gap (not a coder error) and should go back to the planner to decide the fix (update these two call-site literals, and optionally flag the third instance in `admin/settings/page.tsx` for a future pass).

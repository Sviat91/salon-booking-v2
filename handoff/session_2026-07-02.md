# Session: 2026-07-02

## Completed
- **Initialized the DOX tree** (Mode: SINGLE — documentation-only, zero risk of breaking functionality). Created 12 `AGENTS.md` files at durable subtree boundaries:
  - `src/app/AGENTS.md` — routing/pages/auth-guard contract, parent of two children
  - `src/app/api/AGENTS.md` — API route handler conventions (auth checks, validation, error handling, cache invalidation)
  - `src/app/admin/AGENTS.md` — admin/master dashboard role-gating contract
  - `src/components/AGENTS.md` — component library conventions, parent of one child
  - `src/components/booking-management/AGENTS.md` — self-contained client booking-management feature module
  - `src/lib/AGENTS.md` — core business logic/services boundary (cache, availability, notifications, encryption, validation, admin-permissions)
  - `src/config/AGENTS.md` — master config client/server split
  - `prisma/AGENTS.md` — data model, migration workflow, seed files
  - `scripts/AGENTS.md` — operational CLI scripts
  - `tests/AGENTS.md` — Vitest suite structure/mirroring conventions
  - `docs/AGENTS.md` — flags legacy `docs/*.md` as non-authoritative/superseded
  - `handoff/AGENTS.md` — this folder's own purpose (generated by `/sesend`, read by `/sesstart`)
- Replaced the placeholder "not yet indexed" message in root `CLAUDE.md`'s **Child DOX Index** section with real links to all 12 files, nested to reflect parent/child relationships.
- Verified every relative link across all 12 files resolves to a real file (no broken cross-references), and that no leftover `[[memory-style]]` link syntax remained (initially used by mistake, then corrected to plain relative markdown links).
- While writing the docs, found and flagged (without fixing — out of scope for a docs-only pass) several places where root `CLAUDE.md` no longer matches the actual code:
  - Google Calendar/Sheets integration appears dead: `GOOGLE_CALENDAR_ID*`/`GOOGLE_SHEET_ID*` env vars have zero consumers under `src/`; `masters.server.ts` no longer holds Calendar/Sheet resource IDs, just duplicated name/avatar.
  - `npx tsx scripts/consent-cli.ts` (listed as a root Command) doesn't exist — only `scripts/create-superadmin.ts` is present.
  - `tests/lib/google/*` and `tests/lib/google-sheets.procedures.test.ts` are named after a `src/lib/google/` module that no longer exists (logic now lives in `src/lib/consent-service.ts`).
  - Root doc says "13 models" in `prisma/schema.prisma`; actual count is 12.

## In Progress
- Nothing left mid-change. The DOX pass is complete and closed out.

## Next Steps
1. **User confirmed (this session): rip out all Google Calendar, Google Sheets, and hardcoded-default-master logic** — but explicitly **not now**. Sequencing: finish the M3 system redesign first, then do this cleanup as its own pass. Do not start this removal without explicit re-confirmation, and expect it to touch: `src/config/masters.ts`/`masters.server.ts` (hardcoded `olga`/`yuliia` defaults), the `GOOGLE_*` env vars, `.env.example`, root `CLAUDE.md`'s Architecture/Env sections, and the stale references flagged above in `src/config/AGENTS.md`, `scripts/AGENTS.md`, `tests/AGENTS.md`.
2. Resume the M3 redesign where the previous session (2026-07-01) left off: diagnose why `Select`'s popup doesn't feel trigger-anchored (see that session's notes on `SelectPositioner`/`data-side` in `src/components/ui/select.tsx`), then continue to **Part 3 (Admin Panel Redesign)** — `AdminSidebar.tsx` Navigation Rail, dashboard M3 stat cards, calendar grid styling.
3. Still deferred: 401 bug on `POST /api/master/schedule/overrides/bulk` when admin edits day-schedule overrides (tracked in memory `project_next_tasks.md`) — fix after the full M3 redesign is done.
4. When editing any file under a path now covered by an `AGENTS.md`, follow the DOX "Read Before Editing" chain from root `CLAUDE.md` before making changes, and do the "Update After Editing" DOX pass afterward per that same file's rules.

## Decisions Made
- **DOX tree scope was kept surgical**: only the root `CLAUDE.md` Child DOX Index section was edited; the Architecture/Commands/Environment Variables sections (which contain the stale Google integration references) were left untouched and instead flagged inline in the relevant child `AGENTS.md` files, since fixing them is a separate scope decision the user needed to make — which they made in this session (see Next Steps #1).
- **Google Calendar/Sheets/default-masters removal is confirmed but explicitly deferred** until after the M3 redesign is finished — do not bundle this cleanup into redesign work, keep it a separate pass.

## Blockers
- None. Next session should pick up the M3 redesign (Select positioning bug first), not the Google-logic removal.

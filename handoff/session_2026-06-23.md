# Session: 2026-06-23

## Completed

### N8N / Google Cleanup (carried over from previous session)
- `src/lib/env.ts` — removed 4 N8N env vars from Zod schema
- `src/app/api/support/contact/route.ts` — stripped N8N webhook call
- `src/app/api/master/contact/route.ts` — stripped N8N webhook call
- `src/config/masters.server.ts` — removed calendarId/sheetId fields and 4 Google Calendar/Sheets functions
- `src/lib/api/error-responses.ts` — removed CALENDAR_ERROR, SHEETS_ERROR, calendarError(), sheetsError()
- `src/lib/reviews.ts` — simplified to return [] (removed Redis/Drive code)
- Deleted: `src/lib/booking-modification-helpers.ts`, `src/lib/cache/sheets-cache.ts`, `src/app/api/reviews/route.ts`, several debug scripts

### Background System (light + dark per-theme)
- `prisma/schema.prisma` — added 11 new fields to TenantConfig:
  - `bgType`, `bgImageUrl`, `bgGradientFrom`, `bgGradientTo`, `bgGradientAngle`, `bgApplyToDark` (migration 20260623000002)
  - `logoFullscreen` (migration 20260623000001)
  - `darkBgType`, `darkBgImageUrl`, `darkBgGradientFrom`, `darkBgGradientTo`, `darkBgGradientAngle` (migration 20260623000003)
- `src/lib/tenant.ts` — added all defaults for new fields
- `src/app/layout.tsx` — light theme uses `html:not(.dark)` selectors, dark theme uses `.dark` selectors; admin panel made transparent with `.admin-layout, .admin-layout main { background: transparent !important }`
- `src/app/admin/settings/BackgroundSection.tsx` — 3-way toggle (Solid/Gradient/Picture) with `prefix` prop for dark theme reuse; button style fixed (bg-card container, bg-muted active); solid tab hidden when `prefix` set
- `src/app/admin/settings/SettingsForm.tsx` — two BackgroundSection instances (light + dark); dirty state tracking; dispatches `settings-dirty` custom event; form has `id="settings-form"`
- `src/app/admin/settings/actions.ts` — all 11 new fields in Zod schema + raw + data

### Logo Editor
- `src/app/admin/settings/LogoEditor.tsx` — logo size slider max increased to 800px (both inline and fullscreen)
- `src/app/admin/settings/HomepagePreview.tsx` — extracted from LogoEditor (new file)

### Sidebar Save Button
- `src/components/admin/AdminSidebar.tsx` — Save button added above user info divider; only visible on `/admin/settings*`; disabled when no changes; uses `form="settings-form"` + `settings-dirty` custom event listener
- `src/app/admin/settings/SettingsForm.tsx` — removed standalone Save button at bottom (sidebar-only now)

### File Size Refactor
- `src/app/admin/settings/FormFields.tsx` — NEW file: extracted ColorRow, ImageUploadField, SubmitButton from SettingsForm
- `src/app/admin/settings/SettingsForm.tsx` — down to 417 lines (was 529)
- `src/app/admin/settings/LogoEditor.tsx` — down to 443 lines (was 559)

### Minor Fixes
- `src/components/ui/card.tsx` — renamed from Card.tsx to fix TypeScript case sensitivity build error
- `src/app/admin/settings/actions.ts` — logoWidth max increased from 500 to 800 to match slider

## In Progress

Nothing actively in progress — all changes are merged and build passes cleanly.

## Next Steps (priority order)

1. **Telegram bot notifications** — contact form routes (`/api/support/contact`, `/api/master/contact`) currently log and return success as placeholder. Need Telegram bot integration to replace N8N.
2. **Reviews feature rewrite** — deleted the Google Drive reviews system. User wants it back in a different form (approach TBD).
3. **Background gradient active state** — verify the background truly shows on ALL pages in production (auth, booking, profile). The `body:has(.admin-layout)` CSS rule is overridden by our injected style; non-admin pages should work via body gradient naturally.
4. **siteUrl field** — per the old plan (`twinkly-giggling-cat.md`), move `siteUrl` from env var to TenantConfig. Not done yet.
5. **Logo fullscreen** — user deprioritized this ("let's skip for now"); implemented the checkbox but the actual full-screen rendering might need revisiting.

## Decisions Made

- **Background per theme**: separate `bgType`/`darkBgType` fields rather than a single "apply to dark" checkbox — user explicitly wanted independent control
- **Admin background transparency**: rather than modifying globals.css or admin layout.tsx, inject targeted CSS overrides from layout.tsx when gradient/picture is active (`.admin-layout, .admin-layout main { background: transparent !important }`)
- **Sidebar save button**: HTML5 `form="settings-form"` attribute approach — button lives in sidebar but submits the settings form; dirty state communicated via `CustomEvent('settings-dirty')`
- **500-line rule**: enforced by extracting FormFields.tsx and HomepagePreview.tsx
- **prefix pattern** in BackgroundSection: `n()` helper capitalizes field name for dark-prefixed inputs; `!prefix` guard suppresses light-only hidden inputs (secondaryColor, bgApplyToDark) in dark mode

## Blockers

None.

## Key File State

| File | Lines | Note |
|---|---|---|
| `src/app/admin/settings/SettingsForm.tsx` | 417 | ✓ under 500 |
| `src/app/admin/settings/LogoEditor.tsx` | 443 | ✓ under 500 |
| `src/app/admin/settings/BackgroundSection.tsx` | ~197 | ✓ |
| `src/app/admin/settings/FormFields.tsx` | ~115 | new file |
| `src/app/admin/settings/HomepagePreview.tsx` | ~100 | new file |
| `src/components/admin/AdminSidebar.tsx` | ~230 | save button added |
| `src/app/layout.tsx` | ~141 | bg overrides per theme |

## DB Migrations Applied
- `20260623000001_add_bg_logo_fullscreen` — bgType, bgImageUrl, bgGradientFrom/To/Angle, logoFullscreen
- `20260623000002_add_bg_apply_dark` — bgApplyToDark
- `20260623000003_add_dark_bg` — darkBgType, darkBgImageUrl, darkBgGradientFrom/To/Angle

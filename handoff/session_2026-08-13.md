# Session: 2026-08-13

## Completed

Continuation of the `Ordiset` landing-page marketing track (2026-08-09 →
2026-08-11 → today) — no Salon Booking product code was touched this
session, only documentation/planning artifacts in this repo.

**Rewrote `ORDISET_DEMO_WIDGETS_PROMPT.md` entirely (attempt #2 at the
prompt-for-external-tool approach).**
- User reported (4 screenshots) that the first pass's output from the
  external landing-page-building tool looked nothing like the real product:
  inconsistent header/icons per page, off-center About Us/Privacy content,
  a completely different (unstyled, light) look for the booking/calendar
  page, demo window not filling its section.
- Read the real app's source directly (`src/app/page.tsx`,
  `src/app/[masterId]/page.tsx`, `BrandHeader.tsx`, `Footer.tsx`,
  `HomeClient.tsx`, `TopNavLine.tsx`, `MasterSelector.tsx`, `LogoDisplay.tsx`,
  `ThemeToggle.tsx`, `LanguageToggle.tsx`, `DayCalendar.tsx`,
  `ProcedureSelect.tsx`, `TodayPromoCard.tsx`, `MasterFooterBlock.tsx`,
  `BlockRenderer.tsx`/`PhotoWidgetRenderer.tsx`/`PageRenderer.tsx`/
  `PhotoGalleryRenderer.tsx`/`StripWidget.tsx`, `LegalPageHeader.tsx`/
  `LegalDocumentView.tsx`, `ui/card.tsx`, `PageBackLink.tsx`,
  `BookingSuccessPanel.tsx`, `SlotsList.tsx`, `tailwind.config.ts`,
  `src/styles/globals.css`, `src/app/layout.tsx`) to extract the actual
  resolved CSS custom properties, Tailwind class strings, and component
  structure, and rewrote the prompt around literal code blocks (exact
  header markup, Card primitive, calendar structure, resolved light/dark
  token tables mapped onto the real Tailwind semantic names) instead of
  prose description. Diagnosed the likely root cause of the sizing bug:
  the widget was probably built as inline components sharing the landing
  page's own CSS/viewport instead of an isolated context, breaking the real
  app's `w-screen`/`-50vw` full-bleed marquee trick; recommended iframe
  isolation (or container-relative full-bleed) as the fix. Confirmed
  `.btn-primary` intentionally renders body-text-color (not white) on the
  accent button background — already correct in the tool's output.

**Pivoted away from the "prompt an external tool" approach entirely after
this second attempt was also rejected as unworkable** (user: two rounds of
this have failed, "либо ты пишешь хуйню ему, либо он делает хуйню").
- Discussed and ruled out iframing the real production instance directly —
  user does not want the demo touching the real DB/backend at all (shared
  state risk from concurrent visitors, already flagged 2026-08-11).
- Landed on a new plan: build a **self-contained, pure-frontend demo widget
  inside this repo** by literally copying the real components (not
  recreating them from a description), swapping all data-fetching for
  static mock data, zero Prisma/DB/API dependency, `localStorage` for local
  persistence while testing (colors/settings visibly stick on reload).
  Verify it the normal way — run it locally, click through in a browser —
  not judged from a spec. Once it looks right, physically cut the finished
  folder out and hand it to the separate landing-page project to be
  **embedded as-is**, never redescribed/reinterpreted by that tool again.
- Wrote the plan to `handoff/ordiset-demo-widget_plan.md` for continuity —
  scope (Home + master booking page in one demo window, "Customize"/admin-
  settings screens in a second, matching the landing page's existing
  Overview/Customize/Mobile/Booking site/Notifications nav tabs), folder
  location (new `demo-widget/` folder in this repo), and the handoff-to-
  landing-page final step. Explicitly not started — user asked to only
  record the plan this session, begin implementation next session.
- Updated memory (`project_ordiset_landing_page.md` + `MEMORY.md` index) to
  capture the full failed-approach history so a future session doesn't
  retry prompting an external tool a third time.

## In Progress

Nothing mid-implementation. `ORDISET_DEMO_WIDGETS_PROMPT.md` is finished
(may still be useful as a source of copy/content — master bios, service
tables, legal text — for the new approach, but its design-implementation
instructions are now superseded by `handoff/ordiset-demo-widget_plan.md`).

## Next Steps

1. Start `handoff/ordiset-demo-widget_plan.md`: create `demo-widget/` folder
   in this repo, copy in the real components listed there (header/nav,
   `Card`, `DayCalendar`, `MasterSelector`, `ProcedureSelect`, `Footer`,
   legal-page layout, relevant admin/settings screens).
2. Replace all `fetch('/api/...')` calls in the copied components with
   static mock data (Marek/Anna + their service lists, already drafted in
   `ORDISET_DEMO_WIDGETS_PROMPT.md`); strip/no-op any real mutations.
3. Build the "Booking site" window (Home + master booking page, locally
   interactive) and the "Customize" window (admin/settings screens) —
   open decision noted in the plan: fully clickable with `localStorage`
   persistence (default) vs. a couple of static key screens only.
4. Verify by running it locally in a real browser, not from a written spec.
5. Once correct: cut the folder out, move it into the separate Ordiset
   landing-page project (keep a backup copy here), tell that project's tool
   to embed it as-is in the preview window.
6. Unrelated, still open and unblocked: long-term `MARKETING_PLAN.md` Фаза 0
   items (Zoho Mail, GCP Places API), and the parked Salon Booking
   language-switch layout-jump fix from 2026-07-28 (reverted, not restarted).

## Decisions Made

- Demo widget must have **zero dependency on the real DB/backend** — pure
  frontend, static mock data, `localStorage` only for local persistence.
  Non-negotiable per the user, driven by the shared-concurrent-visitor
  corruption risk already flagged 2026-08-11.
- Stop writing prompts for the external landing-page tool to
  interpret/recreate the design — after two failed attempts (prose-only,
  then literal-code-in-prompt), the only remaining approach is building the
  widget for real in this repo and handing over a finished artifact.
- Widget lives in a new folder inside the Salon Booking repo during
  development (easiest place to copy real components from), gets
  physically relocated into the landing-page project once finished.

## Blockers

None — next actions are implementation work in this repo, unblocked, to
start next session per the user's explicit "don't implement yet, just
record the plan" instruction this session.

# Review: Admin-configurable theme-toggle icon (client-facing)
**Date:** 2026-08-04
**Verdict:** APPROVED

## Critical/Architectural Issues
(none)

## Minor/Syntax Issues
(none)

## Passed Checks
- [x] Migration `20260804145805_add_theme_toggle_icons` exists, contains exactly two `ALTER TABLE "TenantConfig" ADD COLUMN` statements (`darkThemeToggleIconUrl`, `themeToggleIconUrl`), both nullable TEXT — matches `schema.prisma:284-285` exactly, placed directly after `darkLogoUrl` as planned. It is the newest migration in `prisma/migrations/`, so ordering is correct.
- [x] **D1** — column names `themeToggleIconUrl` / `darkThemeToggleIconUrl` follow the existing unprefixed-light/`dark`-prefixed-dark convention, not the rejected `themeToggleLightIconUrl` naming.
- [x] **D2 (no cascade)** — `ThemeToggle.tsx:43`: `const customIcon = isDark ? config?.darkThemeToggleIconUrl : config?.themeToggleIconUrl`. Each mode reads only its own field; no `||` cross-fallback between light/dark custom icons. Static defaults (`/light.png`/`/dark.png`) are also independent per branch (`ThemeToggle.tsx:61`). Confirmed this deliberately differs from `LogoDisplay.tsx`'s cascade pattern, and the rule is now recorded in `src/components/AGENTS.md:31`.
- [x] **D3** — `ThemeToggle.tsx` uses `useQuery({ queryKey: ['tenant-config'], staleTime: 60*60*1000 })`, not a raw `useEffect`/`fetch`, consistent with `BrandHeader`/`Footer`'s convention and reusing the same cache key (zero extra requests). Local 2-field type `ThemeToggleConfig` declared inline, not centralized — matches D3's instruction not to unify types in this pass.
- [x] **D4 (SVG safety)** — custom-icon branch renders a plain `<img src={customIcon} ... />` (`ThemeToggle.tsx:52-58`); default branch is untouched `next/image` (`<Image src={isDark ? '/dark.png' : '/light.png'} ... />`, same 48×48 dimensions/classes as before). `next.config.mjs` was not touched (verified no `themeToggleIcon`/related edits and no `dangerouslyAllowSVG` reference introduced).
- [x] **D5 (no loading flash)** — `config` starts `undefined`, `customIcon` evaluates falsy, so the `next/image` default branch renders on first paint identically to pre-feature behavior; no skeleton/spinner/null-render added.
- [x] **D6** — new file `ThemeToggleIconsSection.tsx` (97 lines) is self-contained, owns its own two URL/uploading/error states, rendered inside the existing Brand `SettingsSection` in `SettingsForm.tsx` directly after the favicon `ImageUploadField` block (`SettingsForm.tsx:267-271`), with props `themeToggleIconUrl`, `darkThemeToggleIconUrl`, `onChange` matching the `LanguagesSection` contract shape.
- [x] **D7** — both fields reuse `ImageUploadField` from `./FormFields`; `previewTone?: "light" | "dark"` added to `ImageUploadField` (`FormFields.tsx:57,68`) with default `"light"`, applied only to the two preview-tile class strings (`FormFields.tsx:78,89`); favicon call site passes no `previewTone` and is otherwise unmodified — behavior-identical for the existing call site. Dark-icon field passes `previewTone="dark"` (`ThemeToggleIconsSection.tsx:92`).
- [x] **D8** — `uploadIcon` helper in `ThemeToggleIconsSection.tsx:9-33` replicates `SettingsForm.uploadFile`'s shape: `resizeImageIfNeeded(file, 512)` skipped when `file.type === "image/svg+xml"`, POSTs to `/api/upload`, maps failure via `apiErrorKey(json.code)` through `t()`, falls back to `admin.masters.uploadFailed` — never a raw `error` string surfaced to the user.
- [x] **Empty-string → NULL persistence (`actions.ts`)** — Zod schema adds both fields as `z.string().optional().default("")` (line 17-18); `raw` reads them via `formData.get(...) || ""` (line 83-84); the `data` object maps them via `parsed.data.themeToggleIconUrl || null` (line 146-147) — three-way edit is consistent and mirrors `faviconUrl`/`darkLogoUrl` exactly, so "remove" (which sets the section's local state to `""`) correctly persists `NULL` and restores the built-in default on next load.
- [x] **Zero-prop contract** — grepped all `<ThemeToggle />` JSX usages and `import ThemeToggle from` statements: all 9 planned call sites (`Header.tsx`, `HomeClient.tsx` ×2, `PageRenderer.tsx`, `terms/page.tsx`, `privacy/page.tsx`, `support/page.tsx`, `profile/page.tsx`, `profile/edit/page.tsx`, `[masterId]/page.tsx`) remain zero-prop and unmodified by this feature (no `themeToggleIcon`-related string found in any of them). `AdminSidebar.tsx`'s `<ThemeToggle />` imports the unrelated `@/components/ui/theme-toggle`, confirmed untouched and out of scope.
- [x] **Untouched files** — grepped the whole `src/` tree for `themeToggleIcon`; only the 10 files the plan intended to touch contain any reference. `src/components/ui/theme-toggle.tsx`, `LogoEditor.tsx`, `LogoDisplay.tsx` show zero references (consistent with being untouched). `src/app/api/tenant-config/route.ts` confirmed to return the full `TenantConfig` row unchanged, so the new fields flow through automatically without any route edit — correctly left alone per the plan's "pre-existing, not in scope" note.
- [x] **500-line limit** — `SettingsForm.tsx` is 492 lines (plan estimated ~491), `ThemeToggleIconsSection.tsx` is 97 lines, `FormFields.tsx` is 147 lines — all well under 500.
- [x] **`Label` import omission** — verified `ThemeToggleIconsSection.tsx` never renders a bare `<Label>`; the heading uses `<h3>`/`<p>` (lines 57-60) and both `ImageUploadField` instances render their own internal `<Label>` (`FormFields.tsx:73`). Omitting the unused `Label` import is correct given `--max-warnings=0` lint (an unused import would fail lint), not a plan violation.
- [x] **i18n** — all 6 keys (`themeToggleIconsTitle`, `themeToggleIconsDesc`, `themeToggleIconLabel`, `themeToggleIconHint`, `darkThemeToggleIconLabel`, `darkThemeToggleIconHint`) present in `en.json`, `pl.json`, `uk.json` at identical line positions (592-597, right after `faviconHint`), content matches the plan's specified copy in all three languages.
- [x] `src/lib/tenant.ts`'s `DEFAULT_CONFIG` includes both new fields as `null`, placed next to `darkLogoUrl` (lines 26-27) — required for the DB-unavailable fallback path and `admin/settings/page.tsx`'s typed spread to type-check.
- [x] `admin/settings/page.tsx`'s `fullConfig` maps both fields defensively (`c.themeToggleIconUrl as string | null ?? null`), matching the surrounding style.
- [x] `src/components/AGENTS.md` updated per D9 (line 31) with the no-cascade rule, the `<img>`-for-custom/`<Image>`-for-default split, and the "static PNGs are a permanent default, not unmigrated brand assets" carve-out against the "never hardcode a fallback brand asset" rule in the Work Guidance section — correctly prevents a future agent from "fixing" this into a cascade or deleting the fallback.

## Independent verification (orchestrator, post-review)
- `npm run lint` / `npx tsc --noEmit` — clean (coder's report of 45 pre-existing unrelated problems confirmed to be outside this feature's touched files).
- `npm run test` — 291/291 passing.
- `npm run i18n:check` — PASS.

## Summary
This implementation is clean, precise, and fully consistent with every decision (D1-D9) recorded in the plan. The two most failure-prone details — the deliberate no-cascade fallback (D2) and the `<img>`-vs-`next/image` SVG-safety split (D4) — are both implemented exactly as specified and are non-obvious enough that they'd be easy to get wrong; they were verified directly against the rendered JSX. The Prisma migration is minimal, correctly ordered, and matches the schema. The `actions.ts` three-way Zod/raw/data edit correctly round-trips empty string to `NULL` so "remove and save" restores the built-in default. All 9 `ThemeToggle` call sites and every file the plan flagged as must-not-touch were confirmed untouched via targeted grep. File sizes stay under the 500-line limit, i18n keys are complete and aligned across all three locales in identical position, and the DOX update to `src/components/AGENTS.md` correctly records the new exception to the "no hardcoded brand fallback" rule. No critical or minor issues found.
